# 1. Point this to your main ROOT folder
ROOTSYS=C:\root_v6.36.08

# 2. Add bin and lib to PYTHONPATH so Python finds the module
PYTHONPATH=${ROOTSYS}\bin;${ROOTSYS}\lib

# 3. Add bin to the system PATH so Windows finds the DLLs
PATH=${ROOTSYS}\bin;${PATH}