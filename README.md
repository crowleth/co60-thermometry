# GMA

To install: install WaveForms from the Digilent website (make sure, if on Mac, to properly copy over the SDK files - on Linux and Windows they copy over automatically but on Mac often you have to manually copy them). Make sure you have Python installed, as well as numpy and matplotlib. You can then clone this repository and run pulse_height_analyzer.py. Note that this is largely similar to RUT's code, except RUT additionally has the bias voltage monitor.
